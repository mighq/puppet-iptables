# generated by mighq/iptables puppet module, do not edit manually
#
# entries stored in following format (one per row):
# table;CHAIN
<%-
#require 'pp'
#STDERR.puts PP.pp(@data, "")
-%>
<%-
fwdata = @data['v4']

tables = fwdata.keys.sort
tables.each { |table|

  chains = fwdata[table].keys.sort
  chains.each { |chain|
-%>
<%= sprintf('%s;%s', table, chain) %>
<%- 
  }
}
-%>
